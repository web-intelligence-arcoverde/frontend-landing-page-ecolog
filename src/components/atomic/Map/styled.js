import styled from 'styled-components';
import { COLORS } from '../../../constants/COLORS';

export const Container = styled.div`
  width: 90%;
  height: 90vh ;
  justify-self: center;
  align-self: center;
  margin: 64px 0;
  box-shadow: 0 0 20px 2px ${COLORS.gray};
  border-radius: 10px;

`;
